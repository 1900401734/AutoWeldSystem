# 变更记录

本文件记录 AutoWeldSystem 各版本的行为变化，供现场排查"这一版改了什么"时查阅。

版本号规则见 [README 版本管理](README.md#版本管理)：`feat` 升次版本，`fix` 升修订号，破坏性变更升主版本。

发布时同一版本的说明会同时写入对应的 git tag，可用 `git tag -n99 v2.20.1` 查看，或在 GitHub Releases 页面阅读。

## [2.20.1]

_修复说明_

修复**换工序会清空已选程序**，并把**开工工序改为手动选择**。

- **重新选择工序后程序名称不再被清空**。根因在 `SelectProcess` 无条件清空了可选程序列表和已下载的程序详情，而程序列表只按设备号和工单产品工号查询，与工序无关——同一工单换工序的可选程序完全相同。此前操作员选好程序、换一次工序就得重选并重新下载一次。
- **扫描流转卡获取工单后不再自动选中第一道工序**。此前会自动绑定工序列表第一项并回填工序名称、工序号和生产数量，操作员可能在未核对的情况下直接开工。现在三项留空，必须手动选择工序后才能开工；工单本身没有可选工序时仍然告警。
- **未选工序的提示改为"请先选择工序"**。此前复用"当前工单没有可选工序"的文案，而此时工单明明有工序，容易误判为 MES 数据异常。
- 同一张流转卡重复扫码时会显式清空工序号和生产数量，避免残留旧值绕过开工前的非空校验。
- 换工序不再触发 MES 程序列表查询，切换更快。

## [2.20.0]

_更新说明_

**配方名称现在随程序内容上传 MES**，另一台设备下载同款产品程序后可自动找回正确的 PLC 配方号。

- **程序管理保存时把已选配方名称写入程序内容**，排在设定值前面，例如 `{"工位1配方名称":"左侧标准","高度":"12.5"}`。单工位只写工位 1；双工位按实际选择分别写入；选「不适用」或未选的工位不写。数字配方号仍不上传 MES。
- **配方名称是保留字段，不当测试项**。监控页设定值摘要、程序内容表格和整件检测判定都会跳过它，不会出现「工位1配方名称≤左侧标准」，也不会因名称不是数字导致判定失败。手工 JSON 里写成「配方名称」按工位 1 识别。
- **只填了配方名、没填任何最大允许值的程序也会正常上传**，此前这种程序的内容会被清空。
- **开工前「程序内容确认」弹窗显示本次使用的配方名称**，位于测试项表格上方，只读；改配方仍在程序管理进行。旧程序缺少该字段时显示「未指定」，不影响确认开工。
- **下载或从 MES 拉取程序时按配方名称匹配本机 PLC 配方槽位，自动回填工位配方号**。监控页下载时若名称匹配不上会明确报错并列出本机可用配方，不会带着错误配方继续开工；程序管理批量拉取时只记录日志、不中断整批。同名槽位取第一个。
- **配方名称会去掉 PLC 定长字符串的空字符填充**。PLC 按固定长度返回配方名，尾部用空字符补齐，若不处理会写成 `123#\u0000\u0000…` 上传 MES，另一台设备按名称反查槽位时也永远对不上。现在读取 PLC 配方名、写入程序内容和名称匹配三处都会截断到第一个空字符；全部为空字符的空槽位不再混入配方下拉列表，且不影响后续槽位的配方号。
- PLC 下发、回读校验和运行中调和逻辑不变，仍只使用数字配方号。

**升级注意**：已在 MES 上的旧程序不含配方名称，行为与之前一致，仍按本机保存的配方号下发。需要跨设备共享配方关联的程序，请在程序管理重新选择配方名称并同步一次。现场若改了 PLC 配方名称或槽位顺序，仍需回程序管理重选，系统不会按名称自动迁移历史关联。

## [2.19.7]

_修复说明_

修复**整件检测合并显示未跟随方案明细实时预览勾选**的问题。

- **未勾选的测试项不再出现在合并表中**。方案明细树取消勾选“高度”等测试项后，逐面显示本来就不建该列，但合并显示仍按“是否保存/转发/报表/过程参数”建列，因此现场会看到未勾选的高度。现在合并列只保留同时启用实时预览实际值、且仍有业务输出去向的测试项。
- **产品判定和输出通道不变**。取消实时预览只影响监控页合并表和产品历史合并列；本地保存、中心转发、报表、MES 以及程序判定仍按各自通道处理，不会因为界面隐藏某一列而漏判。

## [2.19.6]

_修复说明_

修复**整件检测逐面判定把视觉检测失败误判成 OK** 的问题，并让未配置上限的测试项退出判定。

- **高度为 0 的检测面判 NG**。视觉检测失败约定回传 0，而逐面判定此前只比较上限，0 必然小于上限，于是"没测到"被判成 OK。高度是产品实体尺寸，0 只可能来自采集失败。现场曾出现面1、面3 高度和对称度全为 0 却显示 OK 的情况。
- **某面的测试项全为 0 同样判 NG**，由上一条覆盖，不另设规则。
- **对称度合并值为 0 判 NG**。对称度单面真实为 0 是完全对称，允许；但 A/B 合并值为 0 说明该侧两个面（1/3 或 2/4）都没检测成功。B 行合并值为 0 时 B 行判 NG，A/B 任一行 NG 则产品结果为 NG。
- **未填最大设定值的测试项不参与判定**。此前会报"程序内容缺少最大允许值"并使整次判定失败，因此宽度只要没配上限就无法只做本地留存。现在该项跳过判定，数据照常采集、落本地、转发看板，只是不参与合格判定——现场可据此把数据分成"需上传 MES（按规范判定）"和"仅供本地查阅"两类。参与判定的测试项**全都**没有上限时仍视为无判定依据：正式采集拒绝入库，报表和上传保持原行结果不中断。

**口径变化**：升级后原本因某面高度为 0 而显示 OK 的面将改判 NG，产品结果随之可能由 OK 变 NG；对称度某侧两面同时为 0 的产品同样改判 NG。这是纠正误判，但会改变现场看到的结果和已上传 MES 之后的新数据，已上传的历史数据不变。

## [2.19.5]

_更新说明_

数据管理页「导出测试数据」明确定位为**后台查阅用的明细数据**，与上传给 MES 的报告文件彻底区分开。

- **不再做 A/B 面聚合**。整件检测四面工艺下，此前导出会把四面合并成 A/B 两行、按配置取平均或最大值，并改写行结果，原始实测值无法追溯。现在导出保留 PLC 采集的逐面原始记录：四面就是四行，每面的实测值和结果原样输出，采集点表头也保持「焊点编号/焊点结果」，不切换成 A/B 的「检测面/检测结果」。
- **动态列覆盖历史记录中的全部测试项**。此前导出只按「写入报表」通道取列，而界面历史表格按「本地保存」通道显示，某测试项勾了本地保存但未勾写入报表时，界面看得到、导出里却缺这一列。现在按「本地保存 ∪ 写入报表」取列，列头和取值同源。
- **上传给 MES 的报表口径完全不变**：仍按「写入报表」取列，整件检测仍聚合为 A/B 两行，对外协议和客户模板无任何变化。

## [2.19.3]

_修复说明_

修复**双工位模式下 PLC 报警通知卡片只出现在扩展屏、主屏看不到**的问题。

- 双工位下主屏和扩展屏各有一个监控视图实例，两者共用同一个写死的通知 ID。AntdUI 按 ID 全局去重，后创建的扩展屏实例每轮刷新都会先关掉主屏刚弹出的卡片再弹自己的，因此卡片始终只留在扩展屏。
- 现在通知 ID 按视图实例生成，主屏和扩展屏各自独立显示一张报警卡片，两张卡片内容相同、可分别关闭。
- 「报警属于整台设备、同一视图内双工位聚合成一张卡片」的既有行为不变：同一个屏幕上不会因为双工位而弹出两张卡片。右侧异常摘要、「清除」按钮和报警恢复自动关闭的行为也都不变。

## [2.19.2]

_更新说明_

- 数据管理页「导出测试数据」不再输出末列的「上传状态」。上传状态属于界面查询信息，导出文件用于交付和留档，混入该列会让同一份数据因补传时机不同而内容不一致。
- 本地导出与上传给 MES 的报表现在共用同一套列定义，末列一律是产品结果，两者不再有列差异。
- 上传状态仍可在数据管理页的工单列表、测试数据和报告文件表格中查看，查询方式不变。

## [2.19.1]

_更新说明_

- 数据管理页详情区域的两个页签调换顺序，现为「报告文件、测试数据」；打开数据管理时默认停在「报告文件」页。两个页签的内容和功能不变。

## [2.19.0]

_更新说明_

方案明细的配置项由四个改为**五个互相独立的通道**，术语与数据实际去向一致。原先「保存历史」同时控制本地历史和中心看板转发，现场无法只转发不落本地，也无法只落本地不转发；「启用采集」既是实时预览开关，又被程序判定当作参与项选择器，还是其余输出的保存期前置条件。

- **实时预览**（左侧树勾选，原「启用采集」）：决定哪些数据显示在监控页的实时预览表格 `dgvPreview1/2`。右侧表格中重复的「启用采集」列已移除，实时预览只能在左侧树勾选，避免两个入口互相改写。
- **本地保存**（原「保存历史」）：决定哪些数据保存在本地历史，供历史数据页和产品历史查询。
- **转发看板**（新增）：决定哪些数据转发至中心服务器看板。
- **写入报表**：决定哪些数据写入完工上报时上传至服务器的 xlsx 报表。
- **过程参数**（原「上传 MES」）：决定哪些数据通过接口上传。相邻的「MES 字段名」同步改称「过程参数字段名」。

五个通道互相独立，实时预览不再是其他通道的前置条件。方案明细只要勾选五者中任意一项即可保存；只做实时预览、不落任何库是合法配置，可用于临时观察某个参数。勾选「过程参数」时仍必须填写过程参数字段名。

### 升级注意

- **「转发看板」升级后默认关闭，需在方案明细中重新勾选**，否则中心服务器看板不再显示动态测试项列。此前由「保存历史」驱动的转发行为不会自动迁移。
- **整件检测程序判定模式下，参与合格判定的测试项改由输出通道决定**：实际值配了表达式且勾选了本地保存、转发看板、写入报表或过程参数中任一项的测试项参与判定；只勾实时预览的临时观察项不再参与。升级前若存在「勾了实时预览但未勾任何输出」的实际值项，该项将退出判定，请核对现场配置。
- 参与判定但未勾实时预览的测试项仍会被读取 PLC，以保证实时预览与采集落库的判定结果同源；因此取消实时预览勾选不再减少 PLC 读取量。
- 数据库自动新增四个转发看板字段，不影响既有数据，无需手工迁移。

## [2.18.0]

_更新说明_

- 测试项字典的偏移量表达式新增**减法计算式**，用于焊接前后位移量这类 PLC 没有对应寄存器、但可由两个已有测试项相减得出的数值。写法 `(134-84):S-7_3`：读字节偏移 134 和 84 两个值相减，类型、规则和小数位对两个操作数同时生效。
- 只支持一次减法和两个整数相对偏移。不支持加法、乘除、多操作数、括号嵌套，也不支持 PLC 绝对地址、Bool 类型和结果规则（`-4`）——结果值是 `2=NG/3=OK/4=焊前NG` 的枚举，相减没有意义，这些形态在保存时即被拒绝并给出明确提示。
- **相减后只格式化一次**。两个操作数各自读原始值并按规则缩放，差值再按小数位格式化，避免各自格式化后相减带来的二次舍入。
- 差值为负（焊后小于焊前）**照实输出**。这是真实测量结果，隐藏会掩盖偏移量配反等现场问题。
- 任一操作数读取失败，或 String 类型的值不是完整数值文本时，**本件采集失败**并沿用现有 PLC 失败反馈要求重采，不落库半成品、不把无效值当 0 参与计算。
- 计算式无需配置上限、下限和结果偏移，不参与本地 OK/NG 判定，位移量只作为数值上传。
- 地址预览的「最终地址」列对计算式显示两个操作数地址（如 `DB23.134 - DB23.84`），便于核对配对是否正确；点击测试显示最终计算值。
- 现有表达式解析行为完全不变，不涉及数据库结构调整，升级后旧配置无需改动。

## [2.17.4]

_修复说明_

修复**中心服务器停机期间采集的产品数据永久丢失**的问题，确保设备端历史数据一件不漏地同步到看板。

- 此前连接类异常（服务器关闭、网络不可达）会穿透转发消费循环，任务停留在「上传中」且重试次数已递增。默认重试上限 3 次、退避 10/20/30 秒，因此**中心服务器只要关闭超过约 30 秒**，该时段的产品数据就会被消费查询永久排除，服务器恢复后也不再补传。
- 现按失败性质分类处理：**连接类失败不消耗重试配额**，只推迟 30 秒后继续尝试，服务器恢复后自动补齐，永不进入失败终态；**业务类失败**（如报表字段或表头校验被拒）仍保留 3 次上限，耗尽后转为失败状态提示人工介入——这类请求重试再多次也不会成功。
- 启动时唤醒此前被判死的转发任务，覆盖「失败」和卡在「上传中」两种状态，历史积压数据自动补传。
- 工单完工时增加一次补漏扫描：重新排队该工单尚未成功的产品数据，作为整批数据到位的最后一道保障。只针对本工单未成功的任务，不会重发已成功的产品。
- 逐产品实时转发仍是主通道，看板在生产过程中照常实时更新；上述机制只负责兜住失败的部分。
- 中心服务器长时间不可达期间，服务器日志按「首次失败 + 每 10 分钟摘要 + 恢复」记录，不会刷屏。

## [2.17.3]

_修复说明_

本版修复中心服务器看板收不到设备端产品数据的两个独立缺陷。两者叠加时，第一个让数据发不出去，第二个让发出去的数据被拒收。

**一、转发任务类型被静默改写（设备端）**

- `UploadTaskService.NormalizeTaskType` 漏登记 `CenterProductReport`，落入兜底分支后被改写成 `ProcessParameter`，而中心转发服务按 `CenterProductReport` 查询待发任务，因此永远取不到——任务全部堆在本地库里无人消费。
- 现象：设备端有「中心服务器产品数据转发已入队」日志，但没有后续的转发成功或失败记录；中心服务端收不到任何 `product-report` 请求。心跳和设备状态走另一条链路一直正常，所以看板上设备在线却没有数据。
- 升级后启动时**自动修正历史上被错误归类的转发任务**，重新进入现有重试队列补传，无需手工处理。判定条件 `Target=CentralServer` 只由中心转发写入，不影响 MES 上传任务；补传按队列串行进行，不会一次性冲击中心服务器。

**二、中心报表凭空补出采集点表头（服务端）**

- 采集点表头由设备端**工位级工艺配置**决定（点焊为「焊点编号」，整件检测为「检测面」），中心服务端无从判断。但 `CenterProductReportFormat.BuildDetailColumns` 在设备端未提供表头时会补默认值，且默认值硬编码为非整件检测的「焊点编号」。
- 工单完工更新请求按契约只推进任务状态、不携带列定义，服务端却拿这个空列表补出「焊点编号」写进中心报表文件。此后携带「检测面」的产品数据全部被表头一致性校验拒收，报错「同一中心报表的采集点表头不一致：焊点编号 / 检测面」。
- 现改为：设备端未提供采集点表头时**留空**，不再猜测设备类型；设备端提供表头时原样保留，行为不变。
- 该缺陷只影响采集点表头不是「焊点编号」的设备，因此整件检测设备接入后才暴露；点焊设备的表头恰好与被猜出的默认值一致，从未受影响。

**升级后的现场处置**

已生成的中心报表文件里可能残留错误表头，服务端读取时原样读回，不会自愈。需要停止中心服务端，删除受影响的 `<数据目录>\<设备编号>\<工单号>.xlsx` 后重启，文件会按正确表头重建。已耗尽重试次数（状态为失败）的转发任务不会自动重发，该部分产品数据在设备端本地库完整保留，可按需重新处理。

产品数据的同步字段范围不变：仍由测试方案明细中的「保存历史」决定，与数据管理页历史列表看到的动态列一致；报表或 MES 独占字段不会同步。

## [2.17.1]

_修复说明_

- 「报表小数位」改为**只作用于上传给 MES 的报表**。数据管理页的「导出测试数据」此前与上传报表共用同一段写入逻辑，因此也被该设置改写位数；现在改为沿用测试项配置的采集小数位，与实时预览、历史数据列表看到的位数一致，本地查看和追溯不再出现三处位数不同。
- 实时预览和数据管理列表本来就没有应用该设置，行为不变。
- 上传报表和 MES 过程参数的输出位数不变，不影响已对接的 MES 口径。
- 留空「报表小数位」时两个出口完全一致，因此未配置该项的现场升级后无任何变化。

## [2.17.0]

_更新说明_

- 整件检测的监控页默认视图改为**合并显示、不显示「面结果」列**。合并视图与 MES 过程参数、XLSX 报表口径一致，现场核对不必再做换算；而单面结果与合并后的产品结果判定口径不同，同屏显示容易被误读成互相矛盾。
- **只影响全新安装**。已部署现场的数据库里这两个设置有明确值，升级后保持原样，需要改为新默认时在生产监控页或系统设置页手动切换一次即可（与 v2.12.0「A/B配对聚合方式」改默认的处理方式一致）。
- 「合并显示」和「面结果」两个开关新增独立权限，挂在权限树的「生产监控」下，**默认只对管理员和开发者开放**。这两个开关写的是全局设置，一名操作员误勾选会改变所有人的显示口径，因此不再对操作员和只读角色开放。
- 无权限的角色登录后，监控页的两个快捷开关以及系统设置页的「整件检测四面合并显示」「整件检测逐面结果显示」都不显示；已保存的显示口径照常生效，只是不能切换。
- 旧数据库升级启动一次后，会自动为已有生产监控页权限的管理员补上这两个新权限；后续启动不再补权，不会覆盖管理员手工取消的配置。
- 两个开关仍然只影响界面：面结果照常计算、入库，并进入历史数据、报表和 MES 上传。

## [2.16.0]

_更新说明_

- 数据管理页「导出测试数据」改为**完全按上传报表格式导出**，并在明细末列追加中文「上传状态」（待上传/上传中/已上传/上传失败/重试中/已跳过）。此前导出与实际上传给 MES 的报表是两套独立格式，现场拿导出文件和上传报表对不上，只能人工换算。
- 导出复用生产报表服务的同一条写文件管线，因此客户模板抬头、明细表头行位置、按产品合并、小数位格式化和整件检测 A/B 聚合全部一致；导出动作**不创建也不更新**报表文件记录，不影响真实上传链路。
- 上传状态列**只出现在数据管理页的手动导出中**，发给 MES 的报表文件不含该列。
- **五条行为变化**：① 导出不再受页面「产品结果筛选」影响，始终导出整个工单；② 整件检测设备从逐面 4 行变为 A/B 两行，宽度 B 行显示 `\`；③ 「记录时间」列取消；④ 「工单号」列取消，工单号即流转卡号，已在模板抬头第一行；⑤ 动态列改由**报表配置（ReportEnable）**决定，可能与界面显示列（历史显示配置）不同——某测试项界面看得到但报表未启用时，导出里不再出现；数值按「报表小数位」配置格式化。

## [2.15.1]

_修复说明_

- 修复整件检测「程序计算」模式下报表与 MES 的 A/B 行「检测结果」和「产品结果」互相矛盾的问题。此前行结果按**面记录从严合并**（任一面 NG 则该行 NG），产品结果按 **A/B 聚合值与上限比较**，两套口径必然可能打架：高度取四面最大值，面2 测得 24.56、其余三面检测失败回传 0，产品结果按 24.56 判 OK，但只要面1 或面3 因任何原因判 NG，B 行就显示 NG，现场无法解释。
- 现在行结果改为按**该行的合并值**判定，与产品结果同源，两者不会再冲突。两种情况保持原结果：含焊前 NG 的行（产品结果本身也不走合并值判定），以及程序快照缺项等判定失败的情况（报表导出和 MES 上传不能因此中断）。
- 只在整件检测 + 程序计算下生效。`PLC读取` 模式行为不变——那时软件没有判定依据，不替 PLC 下结论。
- 监控页实时预览的「面结果」**不变**，仍按每一面自己的值判定：逐面视图的意义就是看单面实际情况。嫌与产品结果口径不同造成歧义，可用 v2.12.0 的「整件检测逐面结果显示」开关隐藏该列。
- **两条口径变化**：① 发给 MES 的 A/B 行 `Result` 会变，原本因单面失败上传 NG 的行今后上传 OK，需告知 MES 方，已上传的历史数据不变；② 报表在导出时实时聚合，重新导出旧产品时行结果按新口径计算，与此前已交付的报表不一致。

## [2.15.0]

_更新说明_

- 生产监控页实时预览上方新增「设定值」显示，格式为 `设定值：高度≤16.00 宽度≤20.00 对称度≤0.10`，现场无需再进程序管理页翻查最大允许值。用 `≤` 是因为判定规则为实际值大于最大允许值才 NG。
- 取值来自**开工时固化到任务的程序快照**，与产品判定使用同一份数据，显示值与判定值必然一致；未开工时显示 `--`。开工后即使程序被改动，显示的仍是本次开工生效的那一份。
- 仅整件检测设备显示，其他设备类型隐藏该标签且不占布局宽度。PLC 判定模式下同样显示，方便核对程序参数。
- 宽度不足时末尾省略，鼠标悬停可查看完整内容。为此实时预览上方那一行的弹性宽度由「焊点」列让给「设定值」列，焊点信息改为按内容宽度显示，不会被挤掉。

## [2.14.0]

_更新说明_

- 「特定数量」上传模式改为**滞后一个产品**：凑满批量值后不再立即上传，要等下一个产品采集完成时才提交上一批，避开产品刚采完那一刻。例如批量值为 3 时，采完第 4 件才上传前 3 件；批量值为 1 时，采完第 2 件才上传第 1 件。
- 凑满批量值却等不到下一个产品就完工的情况，由完工时的过程参数补传兜底，数据不会丢。
- 重测重传不受影响，仍绕过批次立即上传——重测属修正性操作，数据正确性优先。
- 双工位各自独立计数，一个工位的产品不会凑另一个工位的批次。「实时上传」和「批量上传」两种模式行为不变。

## [2.13.0]

_更新说明_

- 系统设置的 PLC 一组新增“报表小数位”和“过程参数小数位”，可分别指定 XLSX 报表和 MES 过程参数输出的小数位。**留空表示沿用采集时的小数位**，因此升级后不配置就与之前完全一致。取值范围 0-10，0 表示输出整数。
- 截断还是四舍五入沿用系统设置里已有的“PLC 数值格式处理方式”，不另设模式；关闭“启用 PLC 数值格式处理”时按四舍五入，与采集侧口径一致。两个小数位互相独立，报表配几位不影响过程参数。
- 只作用于测试项数值列。工位、产品编号、面号、面结果、产品结果等固定列，以及 `OK`/`NG` 结果文本和整件检测 B 行宽度的 `\` 标记都不会被改写。过程参数是先按小数位格式化再追加单位。
- **两条使用限制**：① 采集时已按偏移量表达式的小数位格式化后存库，输出端只能减位或补零——采集配 `_2` 存下 `15.88`，报表配 3 位得到的是 `15.880` 而不是 `15.883`，恢复不了被截掉的精度；② 存在二次舍入，采集舍一次、输出再舍一次，与直接从原始值一次舍入可能差一个末位。需要更高精度请改测试项偏移量表达式里的小数位。
- 界面显示不受影响：实时预览和历史数据仍按采集小数位显示，因此界面与报表、过程参数的位数可能不同。

## [2.12.0]

_更新说明_

- 整件检测“程序计算”模式下修复零值误判：视觉检测失败时约定回传 `0`，而判定只比较上限，`0` 必然小于上限，于是“没测到”被判成 OK。现在 A/B 合并后的 `高度`、`宽度` 仍为 `0`（或负值）时判 NG，并在合并视图标红对应列。判定只发生在合并值这一层，单面检测失败仍由其他面的有效值兜住，不会把该面结果或 A/B 行结果污染成 NG。`对称度` 真值可能为 0，不做零值检查。
- `宽度` 改为只取 A 面：现场产品 A、B 两面宽度不同，且过程参数与报表只要求上传 A 面宽度。A 行取面2、面4 的最大值，B 行留空——MES 过程参数 B 行的宽度字段传空字符串（靠 `SideNo` 区分 A/B 行），XLSX 报表 B 行显示 `\`，并且宽度列不再按产品跨行合并单元格。`高度` 仍取四面最大值、A/B 两行同值、可跨行合并。生产监控合并视图不变，`高度`、`宽度` 仍各占一列，宽度显示 A 面值。
- `宽度` 的上限只对 A 面有意义，因此 B 面（面1、面3）的宽度**不参与面级程序判定**。此前面级判定拿每一面自己的宽度去比那个按 A 面设定的上限，B 面宽度不同、超限就把该面判成 NG，进而让 MES 过程参数和 XLSX 报表里 B 行的 `Result` 变成 NG——即使产品结果按 A/B 合并值判定仍是 OK。现在 B 面只用高度、对称度等其余测试项判定。该过滤只在四面整件检测工艺下生效。
- “A/B配对聚合方式”默认值由 `配对平均值` 改为 `配对最大值`。取平均会把检测失败回传的 `0` 拉进结果，反而更容易判 OK；取最大值时单面失败会被有效面自然覆盖。**已部署的现场数据库保留原有设置，需要在系统设置页手动改一次**。
- 新增“整件检测逐面结果显示”开关：逐面模式下可隐藏“面结果”列，面号和逐面实测值全部保留，避免单面结果与合并后的产品结果口径不同造成歧义。可在生产监控页实时切换（实时预览表格上方“面结果”复选框，仅整件检测且未开启合并显示时出现），系统设置页有同名开关且始终同步。该开关只影响界面，面结果照常计算、入库并进入历史数据和报表。
- 口径变化提示：XLSX 报表在导出时实时聚合，不是导出后固化。上述改动生效后重新导出**以前的产品**，宽度会从“四面最大值”变成“A 面最大值”、B 行变成 `\`，对称度按新的默认聚合方式计算，与此前已交付的报表不一致。中心端报表为逐面明细口径，宽度不再按产品级最大值覆盖，改为显示四面各自的原始值。

## [2.11.0]

_更新说明_

- “实时编号来源”选为“程序判断”时，实时预览不再从 PLC 读取实际数量与预设数量：已完成数量按检测结果连续推算（面结果为 OK 或 NG 才算完成，中途未完成即停止计数，PLC 残留结果不会让计数跳号），预设数量取产品工艺配置的数量。实时预览表格同步改为逐面出现，只显示已完成的面，不再一次铺满全部行。选为“PLC读取”时行为与之前完全一致。
- 系统设置两处文案改为通用表述，兼顾检测设备与点焊设备：“实时焊点编号来源”改为“实时编号来源”，“启用 PLC 字符串数值处理”改为“启用 PLC 数值格式处理”（后者自 v2.10.1 起已同时作用于数值类型，原文案已不准确）。

## [2.10.1]

_修复说明_

- 数值类型（Int16/Int32/Float）的 PLC 读值此前只按偏移量表达式的小数位格式化，固定四舍五入，忽略系统设置里的“PLC字符串数值处理方式”；只有 String 类型才遵守该设置。结果是把处理方式选为“截断”时，实时预览和新采集的产品历史仍按四舍五入显示，与 MES 上传、XLSX 报表的数值对不上。现改为两者共同作用：小数位仍由偏移量表达式决定且始终生效，截断还是四舍五入由系统设置决定，界面与上传、报表口径统一。
- “启用PLC字符串数值处理”关闭时数值类型按四舍五入处理，与本次修复前的行为一致；已入库的历史数据保持原样，本次修复只影响此后新采集的数据。

## [2.10.0]

_更新说明_

- 整件检测（四面）新增“整件检测四面合并显示”开关，开启后生产监控的实时预览和产品历史把四面数据合并成一行显示，列为 `高度`（四面最大值）、`宽度`（四面最大值）和按 A/B 配对的 `对称度A`（面2、面4）、`对称度B`（面1、面3），使界面显示与 MES 过程参数、XLSX 报表口径一致。四面明细照常入库，可关闭开关回到逐面显示。开关只影响界面，不改变上传和报表行为。
- 合并显示可在生产监控页实时切换：实时预览表格上方（“实时采集正常 产品编号：…”那一行右侧）有“合并显示”复选框，勾选或取消后两个表格立即重建，无需重启程序，也不必进系统设置页。切换结果会记住，下次启动保持上次视图；系统设置页的同名开关与监控页复选框始终同步。该复选框仅在过程参数设备类型为整件检测时显示。
- 合并显示下产品判 NG 时，实时预览会把超出程序设定值的那一列标成红底白字（例如只有 `对称度B` 超限就只标该列，`高度` 保持原色），不必取消合并逐面排查。该提示依据程序判定结果，只在“检测结果来源”为“程序计算”时出现；“PLC读取”模式下 PLC 只返回结果不返回依据，不做标红。产品历史表暂不标红，需要定位到面时取消合并查看四面明细。
- 新增“A/B配对聚合方式”设置：`配对平均值`（默认，与旧版本一致）或 `配对最大值`。高度和宽度始终取四面最大值，不受该设置影响。该设置会改变上传和报表数据，存在未完工任务时禁止切换。
- 整件检测选择“程序计算”时，产品 OK/NG 改为按 A/B 合并值与程序设定值比较判定（A、B 两行分别判定后取并），与上传和报表使用同一组数据；报表和 MES 的 A/B 行结果同样取自该合并值判定，监控页逐面明细仍按面判定并保留。
- 新增 PLC 地址逻辑键 `product_result_feedback`（产品结果反馈）：四面采集入库后回写产品判定结果，`3=OK`、`2=NG`。地址未配置或 PLC 未连接时跳过回写，不影响采集握手；该回写不是握手信号，写失败只记录日志不重试。

## [2.9.4]

_修复说明_

- PLC 未连接时不允许在线或离线开工；已有任务仍可进入完工数量读取流程，按系统设置决定是否弹出人工输入框。

## [2.9.3]

_修复说明_

- 开工前必须确认相关 PLC 工位已连接；PLC 未连接时在线和离线开工均不可执行。已有任务的完工流程仍按完工数量读取和输入弹窗配置处理。

## [2.9.2]

_更新说明_

- 将“按产品工号筛选程序”从系统设置移动到生产监控工单信息区，并为产品工号筛选、双工单开关增加 ToolTip 说明；单工位自动禁用双工单。

## [2.9.1]

_修复说明_

- MES 完工上报的“实际工作时长”统一保留两位小数：整小时也上报为 `1.00` 这样的两位形式，第三位按四舍五入进位；不足约 18 秒的任务上报 `0.00`。规则只作用于本次升级后新产生的完工上报，队列中已排队的历史补传数据原样保留。

## [2.9.0]

_更新说明_

- 数据管理“测试数据”页签新增分页，默认每页 20 个产品，可切换 20/50/100，单个工单产品很多时不再一次铺满表格。
- 测试数据的产品节点改为默认折叠，工具栏新增“展开全部/折叠全部”按钮；该展开状态在翻页、改产品结果筛选和改排序后保持，切换工单时回到折叠。

## [2.8.2]

_修复说明_

- 修复“待上传数据”各页签“处理消息”列中英文混排的问题：开工信息、完工信息、过程参数、报告文件、工单状态和设备状态六个页签的处理消息统一显示为中文，历史记录一并生效。

## [2.8.1]

_修复说明_

- 修复离线开工、离线完工点击后鼠标长时间变忙的问题：离线时不再同步等待必然失败的设备状态上报，该状态仍写入 JSONL 并进入补传队列。
- 离线开工、离线完工不再显示“正在开工上报”“正在完工上报”，改为直接给出“本地开工成功”“本地完工成功”；在线上报的中间态提示保持不变。

## [2.8.0]

_更新说明_

- 程序管理左侧列表新增分页，默认每页 20 个产品工号，可切换 20/50/100，设备存放上百个加工程序时不再一次铺满列表。
- 程序列表改为按更新时间倒序排列，最近保存或同步的程序排在最前；同一工号下的子程序同样最近更新在前。
- 程序管理工具栏重排：原独立的“刷新”按钮和搜索框合并为带搜索、刷新按钮的查询控件，“保存后立即同步”复选框改用 AntdUI 复选框统一观感，操作方式和默认勾选不变。
- 程序管理右侧编辑区各行标签列宽度统一，输入框和下拉框的左边界与宽度完全一致，不再出现配方名称行比其它行窄一截的情况。

## [2.7.4]

_修复说明_

- 修复离线任务过程参数补传未使用本地 32 位任务 ID 的问题；MES 恢复后会按开工、过程参数、完工、工单状态、报表和设备状态顺序自动补传。
- 离线任务在线完工后立即尝试当前工单补传；待上传数据的工单信息页签支持选择工单后一键重试全链路。
- 修复补传成功后数据管理页面仍显示“待上传”的问题，工单上传状态会随任务结果持久化并刷新。
- 修复 PLC 报警通知点“清除”后卡片仍留在屏幕上无法清除的问题；报警通知不再区分工位，整台设备只弹一张卡片，标题去掉工位号。
- 监控页“生产指标”移除“工单数量”行，该数值已在工单信息区显示；工单数量仍作为“达成率”分母，指标区高度同步收缩一行。
- 修复未开工和完工上报后采集预览仍残留空表头的问题；无运行任务时采集预览的行与列一起清空，不再显示“焊点序号/焊点结果/提示”空表。
